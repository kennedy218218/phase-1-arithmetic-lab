// Multiplication
let num1 = 31, num2 = 2, multiply = num1 * num2; // Should be 62

// Random integer greater than 0
let random = Math.floor(Math.random() * 100) + 1; 

// Modulo operation
let num3 = 16, num4 = 6, mod = num3 % num4; // Should be 4

// Maximum number in a set
let max = Math.max(1, 5, 10, 15, 20); // Should return 20

// Export for testing (if needed)
module.exports = { multiply, random, mod, max };





